# Glossary

## Project-Specific Terms

| Term | Definition | Context / Usage |
| --- | --- | --- |
| **[示例] Tenant** | 租户，多租户系统中的逻辑隔离单位 | 所有业务数据按 tenant_id 隔离 |
| **[示例] Context** | 请求上下文，包含当前用户、租户、语言等信息 | 贯穿全链路，用于权限控制和国际化 |
| `<term>` | `<定义>` | `<使用场景>` |

> **注意：** 上方示例行仅供参考，初始化完成后请删除示例行，只保留真实的术语定义。

## Domain Concepts

| Concept | Description | Related Modules |
| --- | --- | --- |
| **[示例] Order** | 订单，用户购买行为的业务实体 | OrderService, PaymentService |
| **[示例] Shipment** | 发货单，物流运输的业务实体 | ShipmentService, LogisticsProvider |
| `<concept>` | `<描述>` | `<关联模块>` |

> **注意：** 上方示例行仅供参考，初始化完成后请删除示例行，只保留真实的概念定义。

## Abbreviations

| Abbreviation | Full Form | Meaning |
| --- | --- | --- |
| **[示例] SLA** | Service Level Agreement | 服务等级协议 |
| **[示例] API** | Application Programming Interface | 应用程序编程接口 |
| `<abbr>` | `<全称>` | `<含义>` |

> **注意：** 上方示例行仅供参考，初始化完成后请删除示例行，只保留真实的缩写定义。

---

> **维护规则：**
> - 新增业务术语、领域概念时，同步更新此文档
> - 首次在文档中使用新术语时，考虑添加链接指向此处
> - 定期 review，删除已废弃的术语
